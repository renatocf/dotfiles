# '.gitignore' made for dotfiles

# First of all, ignore everything in your directory
*

# Now, let's make git looks to the dot files
# we want to mantain with it:

# README
!.README.md
    
# Git files (including this .gitignore)
!.gitconfig
!.gitignore
!.gitignore_global

# Vim files (.vimrc and .vim directory)
!.vim/
!.vimrc

# Emacs files (.emacs)
!.emacs

# Bash files (.bashrc and .profile)
!.bashrc
!.profile
!.bash_aliases
!.bash_helpers
!.bash_profile

# Mercurial file (.hgrc)
!.hgr
